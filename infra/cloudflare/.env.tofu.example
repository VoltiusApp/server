# Lives at $ROOT/voltius-tofu/.env.tofu, mode 600, and is carried by the secrets
# bundle. The IDs are not secrets; they are here so one file is all `apply` needs.
CLOUDFLARE_API_TOKEN=
TF_VAR_account_id=
TF_VAR_zone_id=
